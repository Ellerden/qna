$('.answer-errors').html('<%= render 'shared/errors', resource: answer %>');
<% if answer.persisted? %>
  $('.answers').append('<%= j render answer %>');
  //$('.new-answer #answer_body').val('');
  $(".new-answer").trigger('reset');
 // ТУТ ЕЩЕ НАДО СХЛОПНУТЬ ССЫЛКИ
 // ТУТ ЕЩЕ НАДО ОЧИСТИТЬ FILENAMES ПОСЛЕ ЗАГРУЗКИ ФАЙЛОВ
<% end %>
